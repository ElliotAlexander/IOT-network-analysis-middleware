version: '3.3'

services:
  postgres:
    image: postgres
    restart: always
    build:
       context: ./postgres-sql/
    ports:
       - "5432:5432"
    environment:
      POSTGRES_PASSWORD: tallest-tree 
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
